#include <stdio.h>

int main(){

char st[32];
gets(st);  // taking input
puts(st); // printing input and moves cursor on next line where as printf(" %s", string); doesnt move to next line
return 0;

}